{
  "name": "profile-pipeline",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:no-pdf": "node scripts/build.mjs --no-pdf",
    "build:weasyprint": "node scripts/build.mjs --engine=weasyprint",
    "check": "node scripts/build.mjs --check",
    "migrate": "node scripts/migrate-markers.mjs"
  },
  "devDependencies": {
    "@fontsource/lato": "^5.2.5",
    "@fontsource/montserrat": "^5.2.5",
    "playwright": "^1.49.0"
  }
}
